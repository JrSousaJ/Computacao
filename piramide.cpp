#include<bits/stdc++.h>

using namespace std;

int main()
{
  while(scanf("%lf%lf%lf%lf",&a,&b,&c,&d))
  {
  if(!a && !b && !c && !d)break;
  double k=((a/2)+b)/d;
  double ans=k*c;
   printf("%.5lf\n",ans);s
  }
}
